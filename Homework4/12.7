#Jai Kapoor 1901832
def get_age():
    age = int(input())
    # TODO: Raise exception for invalid ages
    if age < 18 or age > 75:
        raise ValueError('Invalid age.')
    return age
    
# TODO: Complete fat_burning_heart_rate() function
def fat_burning_heart_rate(age):
    heart_rate = .70 * (220 - age)
    return heart_rate
    
if __name__ == "__main__":
    # TODO: Modify to call get_age() and fat_burning_heart_rate()
    #       and handle the exception
    if get_age != 'q':
        try:
            age = get_age()
            heart = fat_burning_heart_rate(age)
            print('Fat burning heart rate for a', age, 'year-old:', heart, 'bpm')
        except ValueError as i:
            print(i)
            print('Could not calculate heart rate info.\n')
